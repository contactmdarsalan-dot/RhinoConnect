# RhinoConnect Infrastructure

Deployment and infrastructure files will live here.

Planned folders:

- `docker/`: local and production Docker files
- `terraform/`: cloud infrastructure definitions
- `nginx/`: reverse proxy and routing config
